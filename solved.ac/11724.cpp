#include <iostream>
#include <vector>
#include <map>

using namespace std;

int main() {
    int N,M;
    cin >> N >> M;
    /*
    N : 정점의 개수
    M : 간선의 개수
    */
    map<int,vector<int>> graph;

    for (int i=0;i<M;i++){
        int u,v; // 간선의 양 끝점
        cin >> u >> v;
        if (graph.find(u))
    }




}